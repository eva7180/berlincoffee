<% if @order_items.any? %>
  <table class="table table-borderless">
  	<thead>
    	<tr>
	      <th scope="col">Img</th>
	      <th scope="col">Product</th>
	      <th class="text-right" scope="col">Unit Price</th>
	      <th scope="col">Quantity</th>
	      <th class="text-right" scope="col">Total Price</th>
	      <th scope="col"></th>
	    </tr>
	  </thead>
	  <tbody>
	  	<% @order_items.each do |item| %>
	    <tr>
	      <td>  
	      	<div class="product-img">
    				<%= image_tag(item.product.image_url) %>
  				</div>
  			</td>
	      <td><%= item.product.name %></td>
	      <td class="text-right"><%= number_to_currency item.product.price %></td>
	      <td>
	      	<%= form_for item do |f| %>
	      	<div class="form-inline my-2 my-lg-0">
    				<div class="form-group">
            	<%= f.number_field :quantity, placeholder: item.quantity, class: "form-control form-control-sm mr-sm-2" %>
            	<%= f.submit "Update", class: "btn btn-sm " %>
          	</div>
        	</div>
          <% end %>
        </td>
	      <td class="text-right"><%= number_to_currency (item.product.price * item.quantity) %></td>
	      <td><%= link_to ('<i class="fas fa-trash-alt"></i>').html_safe, order_item_path(item), :data => {:confirm => "You sure?", :method => "delete"} %></td>
	    </tr>
	    <% end %>
	    <tr>
	    	<td class="text-right" colspan="5"><strong><p>Cart Total: <%= number_to_currency current_cart.total_price %></p></strong></td>
	    </tr>
	  </tbody>
	</table>
<% else %>
	<p>Your shopping cart is empty.</p>
<% end %>
<%= button_to "Continue Shopping", products_path, method: :get,  class: "submit-btn" %>