// hide new_comment form and display message instead:
$('#new_comment').html("Thank you for your review!");

//render the _comment partial before all other comments
$('.product-comments').prepend("<%= j render @comment %>")

//update average rating with AJAX
$("#average-rating").attr('data-score', "<%= @product.average_rating %>");