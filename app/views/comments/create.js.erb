// hide new_comment form and display message instead:
$('#new_comment').html("Thanks for leaving a review!");

//render the _comment partial before all other comments
//$('#product-comments').prepend("<%= j render @comment %>") -->  dynamically display a comment on the product page after the user makes an AJAX call to post a new comment

//update average rating with AJAX
//$("#average-rating").attr('data-score', "<%= @product.average_rating %>");

